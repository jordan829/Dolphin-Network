using UnityEngine;
using System.Collections;

public class ActionLabel : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//if (controller.Frame ().Hands.Count == 2 && (rightHand.PinchStrength == 1.0 && leftHand.PinchStrength == 1.0)) {
		//}









		/*GameObject taggedText = GameObject.FindWithTag ("CurrAction");
		if(taggedText != null) {
			taggedText.GetComponent<UnityEngine.UI.Text>().text = "booo";
		}*/
	}
}
